<% if user_signed_in? %>
	<h1> You're logged in!</h1>
	<% else %>
		<div class="hero-unit">
			<h1>FuelOperator Homepage</h1>
			<p>
				Website under construction, coming soon. 
				<%= link_to "FuelOperator", "http://fueloperator.com" %>
			</p>

			<p>
				What to know when we go live?  
				<%= link_to "Sign Up Now", new_user_registration_path, class: "btn btn-warning btn-large" %>
			</p>
		</div>
<% end %>